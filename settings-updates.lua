data.raw["double-setting"]["lrm-instant-crafting-interval"].allowed_values = {1}
for i = 2, 100, 1 do
  table.insert(data.raw["double-setting"]["lrm-instant-crafting-interval"].allowed_values, i)
end
for i = 150, 500, 50 do
  table.insert(data.raw["double-setting"]["lrm-instant-crafting-interval"].allowed_values, i)
end
